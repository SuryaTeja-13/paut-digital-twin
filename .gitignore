# Auto-generated / environment files excluded from version control.

# build artifact (a zip of the repo itself — regenerable)
paut-digital-twin.zip

# python bytecode / environments
__pycache__/
*.pyc
.venv/
venv/
.env
.ipynb_checkpoints/

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/

# scratch / temp
scratch_*.py

# tool caches
.pytest_cache/
.ruff_cache/

# preprocessed array cache — regenerable from data/raw via build_dataset.py
data/processed/**/*.npy

# quarantined mislabeled images (kept locally as data-cleaning evidence, not shipped)
data/quarantine/

# stray debug dumps
/defects.json

# transient, regenerable XAI outputs (the _summary.csv aggregate is tracked)
data/processed/xai/*.json
data/processed/xai_overlays/
